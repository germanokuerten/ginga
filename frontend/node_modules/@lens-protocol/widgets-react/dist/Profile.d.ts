/// <reference types="react" />
import { Theme } from './types';
export declare function Profile({ profileId, ethereumAddress, handle, onClick, theme, containerStyle }: {
    profileId?: string;
    handle?: string;
    ethereumAddress?: string;
    onClick?: () => void;
    theme?: Theme;
    containerStyle?: {};
}): JSX.Element | null;
