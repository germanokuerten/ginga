/// <reference types="react" />
import { Theme, Size } from './types';
export declare function FollowOnLens({ handle, theme, size, title }: {
    handle: string;
    theme?: Theme;
    size?: Size;
    title?: string;
}): JSX.Element;
