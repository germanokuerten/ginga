/// <reference types="react" />
import { Theme, Size } from './types';
export declare function ShareToLens({ content, url, via, hashtags, theme, size, title }: {
    content: string;
    url?: string;
    via?: string;
    hashtags?: string;
    theme?: Theme;
    size?: Size;
    title?: string;
}): JSX.Element;
